<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pang.spacedeliveryfordeliverman">

    <uses-feature android:name="android.hardware.camera" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".deliverman.DeliLogin" />
        <activity
            android:name=".deliverman.MenuActivity"
            android:label="@string/title_activity_res_menu" />
        <activity android:name=".deliverman.Home" />
        <activity android:name=".deliverman.FillFood" />
        <activity android:name=".deliverman.profile" />
        <activity android:name=".deliverman.DeliRegister" />
        <activity android:name=".deliverman.UpdateFood" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".deliverman.getResLocationsMapsActivity"
            android:label="@string/title_activity_get_res_locations_maps" />
        <activity android:name=".Fragment_main" />
        <activity android:name=".deliverman.Notification" />
        <activity android:name=".deliverman.UpdateProfile" />

        <!-- Steps to replace file:// uri with content:// uri: -->

        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />

        <!-- Steps to replace file:// uri with content:// uri: -->

        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

        <activity android:name=".deliverman.deliInfo" />
        <activity android:name=".deliverman.getLocationsMapsMydeli"></activity>
    </application>

</manifest>